<div class="page-header">
  <h1>Goals</h1>
</div>

<% _.each(categoryTypes(), function(type) { %>
  <h4><%= type.label %></h4>
  <div class="list-group" id="<%= type.id %>"></div>
<% }) %>

<div class="form-group">
  <button class="btn save-all">Save</button>
</div>
